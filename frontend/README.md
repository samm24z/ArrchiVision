# ArchiVision Frontend (Next.js + Tailwind + R3F)

## Setup
```bash
cd frontend
npm install
# configure backend URL:
echo 'NEXT_PUBLIC_API_URL=http://localhost:8000' > .env.local
npm run dev
```

Open http://localhost:3000
